@model List<Employee>
@{
    Layout = "_AdminLayout";
}

<div class="col-sm-12">
    <div class="page-title-box">
        <div class="float-end">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="#">Metrica</a>
                </li><!--end nav-item-->
                <li class="breadcrumb-item">
                    <a href="#">Pages</a>
                </li><!--end nav-item-->
                <li class="breadcrumb-item active">Starter</li>
            </ol>
        </div>
        <h4 class="page-title">Starter</h4>
    </div><!--end page-title-box-->
</div><!--end col-->

<div class="col-lg-12">
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <div>
                <h4 class="card-title">Striped rows</h4>
                <p class="text-muted mb-0">
                    Use <code>.table-striped</code> to add zebra-striping to any table row
                    within the <code>&lt;tbody&gt;</code>.
                </p>
            </div>
            <a asp-controller="Employee" asp-action="Add" class="btn btn-success">Add employee</a>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Name</th>
                            <th>Surname</th>
                            <th>Fathername</th>
                            <th>Email</th>
                            <th class="text-end">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var employee in Model)
                        {
                            <tr>
                                <td>
                                    @*<img src="assets/images/users/user-3.jpg" alt="" class="rounded-circle thumb-xs me-1"> *@
                                    @employee.Code
                                </td>
                                <td>@employee.Name</td>
                                <td>@employee.Surname</td>
                                <td>@employee.FatherName</td>
                                <td>@employee.Email</td>

                                <td class="text-end">
                                    <a asp-controller="Employee" asp-action="Edit" asp-route-id="@employee.Id">
                                        <i class="las la-pen text-secondary font-16"></i>
                                    </a>
                                    <a asp-controller="Employee" asp-action="Delete" asp-route-code="@employee.Code">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div> <!-- end col -->